name: Kernel Build1

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Шаг 1. Клонирование репозитория
    - name: Checkout kernel source
      uses: actions/checkout@v4

    # Шаг 2. Настройка MemKernel
    - name: Set up MemKernel as a module (M-OUT) with specified name
      run: |
        curl -LSs "https://raw.githubusercontent.com/Poko-Apps/MemKernel/main/kernel/setup.sh" | bash -s M dogi

    # Шаг 3. Сборка ядра
    - name: Android kernel build
      uses: lemniskett/android-kernel-actions@master
      id: build
      env:
        NAME: Loganysl
      with:
        arch: arm64
        compiler: clang/14
        defconfig: vendor/xiaomi/miatoll_defconfig
        image: Image.gz-dtb

    # Шаг 4. Проверка существования файлов
    - name: Verify kernel and module files
      run: |
        echo "Checking Image.gz-dtb"
        if [ ! -f "./out/arch/arm64/boot/Image.gz-dtb" ]; then
          echo "File not found: Image.gz-dtb"
          exit 1
        fi
        echo "Checking memkernel.ko"
        if [ ! -f "./out/drivers/memkernel/memkernel.ko" ]; then
          echo "File not found: memkernel.ko"
          exit 1
        fi

    # Шаг 5. Создание ZIP-архива
    - name: Create ZIP archive
      run: |
        mkdir -p release
        cp ./out/arch/arm64/boot/Image.gz-dtb release/
        cp ./out/drivers/memkernel/memkernel.ko release/
        cd release
        zip -r kernel_package.zip *

    # Шаг 6. Загрузка нового ZIP-архива как артефакта
    - name: Upload Kernel ZIP
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-Package
        path: release/kernel_package.zip
